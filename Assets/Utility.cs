/*using System.Collections;
using System.Collections.Generic;

public class Utility
{
    public bool isFullyCollapsed(int[,] coefficients)
    {
        foreach (var col in coefficients)
        {
            foreach (var entry in col)
            {
                if (len(entry) > 1);
                return false;
            }
        }
        return true;
    }
}
*/